package com.diveroid.camera.ui

import android.content.Context
import android.opengl.GLSurfaceView

class FilterSurfaceView(context: Context): GLSurfaceView(context) {
    // TODO: Android에서 제공해주는 GLSurfaceView 로 교체 가능한지 체크
    // TODO: 일전에 ELS14를 사용하기 위해 별도로 처리한 것을 파악됨.
}